image: continuumio/miniconda3:4.7.12

before_script:
  - conda env create --force --file environment.yml
  - source activate hydra

test:
  script:
    - unzip bin/ScienceBirds_Linux.zip -d bin/
    - python -m pytest
